# HTMLScope 版本紀錄

版本號顯示於工具列左上角。規則：每次改版 +0.1，並在此記錄。

## Landing page — 2026-08-26（首頁用詞偏技術/行銷向，四個角色從不同角度撞上同一件事）

7 角色可用性測試裡有 4 位角色從不同方向指向同一個問題：國二學生一開始看到第一行的「HTML」
就想關掉分頁；行政上班族與大學教授都在標題「HTML檔」處愣了一下；工程師則是反方向的抱怨——
行銷文案拖慢了他找到真正能操作按鈕的速度。首頁第一屏的內容量與詞彙難度,沒有針對
「第一次來、只想知道這是不是我要的工具」的訪客做分層。

- **修正：hero 區的 eyebrow 與副標重新排序/改寫**——原本第一行就是「單一 HTML 檔」，現在把免安裝、免帳號這類普遍讓人安心的話放前面，「HTML 檔」留在句尾（資訊沒有少，
只是不再是訪客讀到的第一個字）
- **修正：副標不再以「打開任何 HTML 文件」開頭**，改成「打開一份報告、會議記錄，或隨手貼上
  一段文字」——用一般人認得的情境詞，同時把「貼上文字」這條路徑從原本只出現在 microcta 列
  最後一項，提早到訪客一定會讀到的第一段文字裡（原本的 microcta 連結保留不動，這是額外補一層）
- **修正：導覽列加上「GitHub」直達連結**——工程師的抱怨是反方向的：不是看不懂，是行銷文案
  太多、要滑到頁面最下面的 Final CTA 才找得到原始碼連結。現在導覽列一開始就有，不用先讀完
  AI 閉環／功能／隱私／比較表四個區塊。實測 862px～1280px 寬度、中英雙語都沒有把導覽列撐爆

## Landing page — 2026-08-26（授權範圍不透明，卡住企業採用決策）

7 角色可用性測試回報：工程師深入查證 GitHub 上的 `LICENSE-zh.md` 才發現企業內部使用需要另外
洽談授權；中小企業主則是在首頁最下方看到極小字「非商業授權」，完全看不懂公司內部使用算不算違規，
兩條路徑撞出同一個結論：**授權範圍對決策者不透明**，原始碼公開不等於商業使用也免費。

- **修正：「哪裡不一樣」比較表下方新增白話說明**——個人使用、學術研究、非營利組織與政府機關內部
  使用完全免費不用問；一般營利公司如果要在團隊裡日常審閱文件，屬於商業使用，需要先聯絡作者。
  放在這裡是因為使用者評估「要不要用這個取代付費 SaaS」時，這正是他們在讀的段落，不用再往下滑
  到頁尾小字、也不用點進 GitHub 挖 `LICENSE-zh.md` 才看得到
- **修正：底部 fileline 的「非商業授權」從純文字改成連結**，點下去跳到上面那段白話說明；企業主原本就是在這行卡住，現在至少有路可以繼續往下讀
- 「聯絡作者」連到 GitHub issue（`github.com/sjjcoder/htmlscope/issues/new`），是目前唯一
  已驗證存在的聯絡管道——**沒有编造 email 地址**（第一版草稿寫了一個不存在的 `hello@sjjcoder.dev`，
  自己抓到後改掉；虛構聯絡方式比沒有更糟，會讓真的想談授權的人寄到不存在的地方）
- 雙語文案的 DOM 結構刻意讓外層容器不掛 `data-en/data-zh`，只讓葉節點（每個 `<span>`/`<a>`）
  各自掛——這個站台的語言切換是整段 `innerHTML` 覆寫，父層若也掛了屬性，切換語言時會把
  裡面剛加的連結整個吃掉（草稿階段真的踩到一次，中英文切換測試後改掉）

## v4.6 — 2026-08-26（『最近開啟』清單殘留陌生檔名，沒有一鍵清除）

7 角色可用性測試裡有 3 位角色在「最近開啟」清單看到自己沒建立過的檔案，行政上班族因此
一度緊張「我的東西是不是不見了/跟別人的資料混在一起」，不敢再繼續操作；工程師則指出
公用電腦情境下，前一位使用者的檔名會留在清單裡給下一個人看到。

（這次測試本身的觸發原因多半是 7 個測試分頁併發共用同一個網域的 localStorage 造成的假象，
不是一般人單機使用會遇到的狀況——但底層顧慮是真的：`localStorage` 本來就是「這台瀏覽器
共用」，不是「這個人專屬」，圖書館、公用電腦、共用工作站都是真實會撞上的場景。）

- **修正：「最近開啟」標題列旁邊加上「🗑 清除全部」**——原本只能逐筆點 ✕ 移除，公用電腦上
  想一次清掉所有痕跡沒有辦法。按下去會先跳確認（顯示筆數），確定後把每一筆的文件內容與
  標註資料一起從 `localStorage` 移除，不留殘骸
- **修正：清單旁補一行說明**——「這份紀錄只存在這台瀏覽器裡，不會同步給任何人」，直接打消
  「資料外洩/跟別人混在一起」的疑慮，同時指出「清除全部」就是解法

實測：清除全部會先問筆數再刪，取消不動、確定則連 localStorage 一起清空且重新整理後不會復活；
既有的單筆 ✕ 刪除功能不受影響。

## v4.5 — 2026-08-26（手機選字後顏色列延遲跳出）

7 角色可用性測試（國二學生用手機）回報：選字後顏色列偶爾沒有立刻跳出來，要再等一下、再碰一次畫面才出現，測試者判斷這是最可能讓國中生放棄改用截圖傳 LINE 問同學的一步。

- **修正：觸控裝置原本只靠 `selectionchange` 單一訊號、350ms 去抖動才顯示顏色列**——手指長按選字後,要等超過三分之一秒畫面才有反應,沒有任何過渡效果時使用者只會覺得「工具當機了」。改成兩個觸發來源並行：①去抖動縮短到 120ms（仍過濾拖曳選取把手時的中間態）②新增 `touchend` 監聽，手指放開的瞬間就檢查一次選取範圍，不必等 debounce 計時器到期，一般的「長按選字→放開」現在幾乎立即反應
- **修正：顏色列出現時加上淡入效果**，讓「畫面有反應」本身就是一種確認訊號，不再是忽然憑空冒出來讓人措手不及。實作上刻意**不用** `requestAnimationFrame` 排到下一幀才變不透明——背景分頁或畫面沒有在合成時 rAF 不保證會觸發，那樣反而會讓顏色列卡在「已顯示但完全透明」的更糟狀態（按鈕在但看不到、點不到）。改用強制觸發一次 reflow（讀 `offsetHeight`）逼瀏覽器先套用透明起點，下一行賦值才會被當成一次「有起點的變化」播放過渡動畫；就算瀏覽器沒有真的播動畫，結果也保證會落在「看得見」，不會卡在中間態
- 尊重 `prefers-reduced-motion`：關閉動態效果的使用者不會看到過渡，直接跳到最終狀態

實測（模擬手機、連續選字兩次）：顏色列跳出從原本的 350ms 起跳，縮短到 touchend 觸發約 50ms、純 selectionchange 路徑約 120ms；桌面滑鼠路徑（mouseup）不受影響；核心的標記/留言流程回歸測試全過。

## v4.4 — 2026-08-18（「含標記版」改名為「傳閱版」；確認匯出選單不整併）

使用者提出兩個問題：① `📤 匯出` 選單跟 `⬇ 含標記版` 這顆獨立按鈕要不要整併成一個？
② 「含標記版」這個詞是否有更好懂的名稱？

**① 不整併。** `⬇ 含標記版`（現改名 `⬇ 傳閱版`）跟匯出選單裡的四項本質不同：
它是唯一支援**來回傳遞、留言自動合併**的檔案，是整個工具最常用的核心動作；匯出選單那四項
（PDF／一頁清單／Markdown／JSON）都是一次性的終點輸出，看完就結束，不會再回頭合併。這正是
本專案一路奉行的原則——核心動作留一顆獨立按鈕、次要動作收進選單（說明頁分層、PDF 匯出調順序
都是同一邏輯）。塞進選單只會多一次點擊，還會讓使用者要在 5 個選項裡分辨「哪個才是可以來回傳的」，
重蹈 💡提議 vs ✏️直接改字 那類命名/圖示混淆的覆轍。

**② 「含標記版」改名為「傳閱版」。** 舊名只描述了「裡面有標記」這個事實，沒說「這是要給同事、
會回來合併」這個目的；英文版其實已經用了目的導向的說法 `Annotated copy`（有標註的副本），
中文卻是字面直譯，少了那個「拿去用」的語感。「傳閱」是台灣辦公室文化裡現成的詞（文件傳給大家看/簽），
直接對上「拿去給同事、他看完傳回來」這個實際用法，也跟 landing page 現有文案「跟同事一起看一份文件」
互相呼應。這不是換一個字而已——按鈕、tooltip、提示條、首次引導、使用說明、外殼下載鈕、以及匯出檔名
後綴全部一起換掉：

- 面板按鈕、外殼下載鈕、提示條、無法自動儲存的警語、30 秒看懂示意圖、基本操作／疑難說明裡的引用全部同步
- 匯出檔名後綴 `sfxAnno` 由 `_標記版` 改為 `_傳閱版`
- **去重複後綴的 regex 同時認得新舊兩種名字**（`/_(標記版|傳閱版|annotated)$/i`，4 處呼叫點）：
  使用者手上已經存在的 `_標記版.html` 舊檔案，重新匯出時後綴會正確換成新名字，不會疊成
  `..._標記版_傳閱版.html` 雙重後綴——這只影響**新產生的檔名**，不影響舊檔案本身的開啟與
  標註偵測（那是看檔案內容裡的 `annoPreset`，跟檔名無關）
- 英文版 `Annotated copy` 本來就是目的導向的說法，這次不動
- landing page（`index.html`）與 `README.md` 的現行說明同步改名；`CHANGELOG.md`
  的過去版本紀錄維持原樣不回溯改寫（那些是各版本當時的用語，屬於歷史記錄）

## v4.3 — 2026-08-17（面板說明文字被浮動鈕蓋住：根因是面板本身沒有裁切）

使用者截圖回報 v4.2 的另一處異常：標註面板右下角的說明文字，被下方的「標註」浮動鈕蓋住，
整塊看起來超出範圍。這跟 v4.0 修過的「面板尺寸夾限」是**同一個功能區but不同的根因**。

v4.0 讓面板的寬高會依視窗大小重新計算，但**從沒讓面板真正裁切自己裝不下的內容** ——
`#annoPanel` 只設了 `max-height`，沒有設 `overflow:hidden`。
`max-height` 只限制盒子本身要多高，**不會裁切「撐爆這個高度」的子元素**；
面板最後一個子元素是 `.ap-hint`（操作提示文字，`flex:none`），只要其餘子元素
（標頭／引導條／篩選列／清單最小高 96px）加起來已經逼近可用空間，`.ap-hint` 就會被往下推、
畫穿面板自己的圓角邊框，蓋到緊鄰在面板正下方的浮動鈕上——這是視覺上唯一會被使用者看見、
覺得「超出範圍」的破綻。夾限計算本身(v4.0 的 `clampPanelSize()`)完全沒有錯，錢包只是
忘了把裝不下的部分蓋起來。

### 修法

- **`#annoPanel` 加上 `overflow:hidden`**：這是唯一的根因修正，讓任何裝不下的內容
  一律在面板自己的邊框內被裁切，不再有機會畫到面板外面、蓋到別的元件
- **`#annoResize`（縮放把手）原本刻意用負值定位**（`left:-2px;top:-2px`）疊在邊框外做抓握感，
  加了 `overflow:hidden` 會被切掉一角——改貼齊邊角（`0,0`），視覺上仍是同一個圓角把手
- **內容真的裝不下時，優先犧牲說明文字讓出空間**：`clampPanelSize()` 現在會在算完尺寸後檢查
  `scrollHeight` 是否仍超出 `clientHeight`，超出就先隱藏 `.ap-hint`（純操作提示，
  別處也有說明，犧牲它換空間給搜尋／篩選／清單這些真正要用的功能）；空間夠時每次都先還原顯示再重新判斷，
  不會卡在永久隱藏
- **這個判斷加了 6px 容錯**：實測 `scrollHeight` 常比 `clientHeight` 多算 1～2px
  （邊框計入方式的捨入差），嚴格用 `scrollHeight > clientHeight` 會讓完全不影響視覺的誤差
  也觸發隱藏，白白犧牲說明文字

實測（1366×720，內容真的很擠的情境）：加上 `overflow:hidden` 後，面板與浮動鈕的重疊
從有變成 **恆為 0**——即使刻意塞進一個裝不下的高度（視窗壓到 372px），內容也只會被安靜裁切在
面板邊框內，不會再蓋到按鈕上。標記、留言、選單定位、面板拖曳縮放全部回歸驗證正常。

## v4.2 — 2026-08-17（SVG 裡的標記會讓文字消失、還會把整張圖扯散）

使用者截圖回報:一份文件的示意圖裡,有一格整格空白、另一格的文字只剩殘缺的片段,
而圖裡其他文字全部跑到圖外面變成一串跑版的段落。原話是「原本文字是看得到的,但做了標記就不見了」。

這跟 v4.1 修的列印問題**是不同的原因**。查出來是兩個疊在一起的缺陷,共同點是:
**那幾個標記被下在 SVG 示意圖裡面。**

- **修正:標記放進 SVG 之後根本不會被渲染** —— `document.createElement('mark')` 造出來的是
  **HTML 命名空間**的元素(實測 `namespaceURI` 是 `xhtml`),插進 SVG 的 `<text>` 裡
  不是 SVG 認得的文字子元素,所以整段不渲染 —— 量出來的盒子是 **0×0**。被標的字就這樣消失了
- **修正:列印編號 `<sup>` 會把 SVG 從中間切斷** —— HTML 規範裡 `sup` 屬於
  「會中斷 foreign content(SVG/MathML)解析」的標籤清單。含標記版把 `<sup class="annoPrintNo">`
  烙進 SVG 之後,檔案**再次開啟時**解析器就從那裡跳出 SVG,後面的 `rect`／`text` 全被
  當成 HTML 內容重新排版 —— 實測一張 15 個元素的圖被扯出 20 幾個裸元素掛在圖外面,
  這正是截圖裡那串跑版文字,**也解釋了為什麼圖上的字會變成殘缺的片段** —— 一段文字被切點分成了兩半,
  前半留在 SVG 裡、後半被丟到外面。

### 修法

- **SVG 內的文字整體排除在可標註範圍之外**(`walker()` 加一條 `closest('svg')` 過濾)。
  這是唯一乾淨的選擇:SVG 文字沒有 HTML 的行內流,要在裡面畫螢光筆得改用 `tspan` 外加背景矩形、
  還要自己算座標,代價遠高於價值
- **選到圖裡的字時直接說明,不再默默沒反應**:「這段字在圖裡(SVG),沒辦法加標記 —— 可以改標圖旁邊的說明文字」
- **匯入時修復既有檔案**:`cleanDocument` 在 `parseFromString` **之前**先從原始字串拆掉
  `<sup class="annoPrintNo">` —— 順序很重要,等拿到 DOM 才清就來不及,圖那時候已經被切爛了。
  解析後再把 SVG 裡殘留的 `mark` 拆掉(它們本來就渲染不出來)
- **產生列印編號時一律跳過 SVG 內的標記**(`beforeprint` 與 `buildStandalone` 各一處防呆)
- **孤兒訊息分流**:原本一律顯示「⚠ 這段原文已被改動」——但這種情況原文根本沒被改,講錯會讓人
  以為自己的檔案壞了。新增 `quoteIsInSvg()`,定位失敗時再確認引文是否出現在某個 SVG 裡,
  是則改顯示「🖼 這段字在圖裡(SVG),沒辦法加標記(留言都還在)」

實測(使用者那份深色文件):圖從被扯散恢復為完整的 **18 個 text／11 個 rect**,
圖上每一格的文字全部回來;SVG 內的 mark 與 sup 從 **6 個→0 個**;
**4 則標註、3 則留言一則沒少** —— 其中 2 則落在圖裡的改列為「在圖裡」並保留全部留言。

> **已經匯出過的舊檔案要用 v4.2 重新匯出一次**才會把烙進 SVG 的 mark/sup 清掉。
> 直接用 v4.2 開啟就已經看得到正確的圖(匯入時會修復),但檔案本身要重新匯出才乾淨。

## v4.1 — 2026-08-17（列印／存 PDF 的三個缺陷 + 舊引擎 CSS 清不乾淨）

使用者拿兩份實際在用的標記版檔案回報:「標了之後居然看不到,連原本的文字都不見了」「輸出成 PDF 時,
會有奇怪的 mark 出現在不應該出現的頁面上」。拿那兩份檔案實測,查出四個各自獨立的問題:

- **修正:深色文件列印出來整份是空白的** —— 瀏覽器列印預設不印背景色,卻照印文字顏色。深色主題的文件
  (背景 `rgb(21,25,26)`、文字 `rgb(230,234,232)`)一到白紙上就變成淺灰字,實測對比只有 **1.21:1**,
  等於整份消失 —— 這正是使用者說的「連原本的文字都不見了」,而且不只標註,是整份文件。
  `beforeprint` 改為量出文件的有效底色,偏暗才掛上 `anno-print-light` 強制白底深字,
  淺色文件(實測對比 14.67:1)完全不動
- **修正:螢光標記根本印不出來** —— 標記靠 `background-color`,而列印對話框的「背景圖形」預設是關的,
  所以標註工具印出來竟然沒有標註。改用 `print-color-adjust:exact` 只針對標記強制印底色
  (不會連整頁背景一起吃墨),再加一條底線當雙保險,因為有些 PDF 產生器不支援這個屬性
- **修正:PDF 上出現孤兒編號** —— 匯出的含標記版會把 `[1]` `[2]` 編號烙進內文,配一份文末靜態對照表
  (給沒有 JS 的環境看)。但列印規則把對照表 `display:none` 掉了,**編號卻沒有一起藏** ——
  結果紙上散落一堆 `[n]` 卻找不到對應的註解。編號與清單是一組的,拿掉那條規則讓兩者一起印
  (有 JS 時引擎載入就已經整個移除靜態附錄,所以不會變成重複)
- **修正:連按兩次列印會疊出兩份對照表** —— `afterprint` 在部分瀏覽器與取消列印時不會觸發,
  而 `beforeprint` 沒有先清殘留就直接追加。改為進場先清空 `printAdded`

### 順帶挖出的更深問題:舊引擎 CSS 一直沒被清乾淨

查上面第三項時發現,使用者的檔案裡**舊引擎的 CSS 還留著 27 條規則**,其中那條舊的
`@media print{#annoStaticApp{display:none}}` 會反過來抵銷這一版的修正。

根因是 `cleanDocument` 的正規表達式只能比對平鋪的規則:
選擇器前面的 `[^{}]*` 跨不過 `@media …{` 的左大括號,**所以寫在 @media 裡的規則永遠掃不到** ——
殘留的 27 條全部在 `@media`(深色模式／列印／pointer:coarse)內。而且原本的樣式清單漏了
`.annoPrintNo`、`.anno-menu`、`.anno-modal-*` 這幾族,根本沒列進去。
這是 README「已知雷」第 1 條記載多年的問題,原來只修好了一半。

- 新增 `stripAnnoRules()`:自己數大括號、遞迴進 at-rule,內容被清空的 `@media` 整塊丟掉。
  選擇器比對要求 `#anno`／`.anno` 後面接大寫字母或連字號,避免誤刪文件自己的 `.announcement` 之類 class
- 注入的引擎樣式改帶 `id="annoEngineCss"`,v4.1 之後匯出的檔案再次匯入時可以整塊移除,不必逐條掃
- 樣式清單改為**用 ENGINE_CSS 的 164 條選擇器逐條驗證涵蓋率**,一條都不能漏 —— 第一版補完後仍漏了
  `.fchip`／`.cdot`／`.dv-*` 這幾族,它們在使用者檔案裡留下 17 條規則。更糟的是那個區塊被清掉
  `#annoBar` 之後,舊的守衛條件(只認 `#annoBar`)就再也認不出它,等於**永久殘留**;守衛已改成認多個標記
- 反向也驗證過 30 個一般文件會用的選擇器(`.hint`／`.note`／`.badge`／`.announcement`／`.app-bar`…)**零誤判**。
  精準度靠兩件事:`#anno`／`.anno` 後面必須接大寫字母或連字號、以及 `\.ap-` 這種點號緊貼前綴的寫法。
  **不要把 `.m`／`.q`／`.ok`／`.sa`／`.note` 這類泛用 class 加進清單** —— 引擎裡它們一律掛在容器底下,已被涵蓋
- 實測(使用者的兩份檔案):重新匯出後只剩 **2 個 style 元素**(文件自己的 + `annoEngineCss`),
  舊引擎殘留 **44 條→0 條**,而文件自己的 CSS(54 條／35 條)**一條都沒被誤刪**,標註、留言全數保留

> 每次來回傳遞都會多疊一層舊引擎 CSS,所以這個問題是會累積的 —— 檔案愈傳愈大、衝突愈來愈多。
> **已經匯出過的舊檔案仍帶著那條會抵銷列印修正的舊規則,要用 v4.1 重新匯出一次才會乾淨。**

## v4.0 — 2026-08-13（面板拖曳出界／版面溢出:四個都是實機量出來的）

使用者回報「標註清單拉動後很容易跑出去界線」,並附上截圖(浮動鈕壓在面板底部說明文字上)。
照著實際操作追下去,發現的不是一個問題而是四個,而且第一個會讓面板**完全卡死**:

- **修正:面板縮放後換小視窗開啟會整個卡死** — 面板靠 `right/bottom` 錨定,所以「尺寸太大」等同於
  「左上角跑到畫面外」,而縮放把手就在左上角。存檔的尺寸原本**無條件套用**,實測在 1280 寬拉大到 1241
  之後換到 860 寬開啟:面板左緣 `-416px`、把手 `-417px` —— 把手抓不到,使用者再也沒辦法把它拉回來。
  修法是把「使用者想要的尺寸」(`wantW/wantH`,永不被夾限改寫)與「這個視窗放得下的尺寸」分開,
  每次都用 `min(想要, 放得下)` 重算。**第一版寫成只夾不還原,結果面板在小視窗被縮小後換回大螢幕
  就永遠停在最小尺寸** —— 分開兩個值才同時解決出界與還原
- **修正:浮動鈕壓住面板底部的說明文字** — 面板的 `bottom:64px` 是「鈕高 39 + 底距 18 + 7 餘裕」
  算出來的魔術數字,**只剩 7px 容錯**。瀏覽器縮放或系統字型放大讓鈕超過 46px 就會疊上去(使用者截圖的狀況)。
  改為新增 `panelLimits()` 實際量測鈕高再算,縮放拖曳的上限也改用同一組值。實測鈕高 39→71→110px
  時面板底部跟著 65→97→136px,重疊一律為 0
- **修正:底部說明文字溢出面板下緣 9.6px** — `ENGINE_CSS` 沒有 box-sizing 重置,
  **引擎 UI 的盒模型是跟著使用者那份文件走的**。在沒有 `*{box-sizing:border-box}` 的文件裡,
  `#annoList` 的 `min-height:96px` 是內容高,再加上下 `padding:6px` 實際變成 108px,
  flex 分配就多出 10px 擠爆底部。給 `#annoList` 補上 `box-sizing:border-box` 即可。
  **AI 產出的報告多半自帶 border-box 重置,所以這個 bug 只在沒設的文件上出現** —— 難怪一直沒被發現
- **修正:短螢幕 + 首次使用引導條 = 說明文字整段掉到面板外(溢出 110px)** — 面板上限寫死 `62vh`,
  在 573px 高的視窗只有 355px,裝不下「標頭 75 + 引導條 120 + 篩選列 105 + 清單最小 96 + 說明 68」。
  溢出的部分直接畫到文件上。改為在該視窗放得下的範圍內允許長到內容需要的高度
  (`min(可用高, max(62vh, scrollHeight))`)。另外 `clampPanelSize()` 原本排在「顯示引導條」**之前**,
  量到的 `scrollHeight` 漏算引導條高度,順序也一併對調。實測:短螢幕首次使用長到 466px 裝下全部,
  關掉引導條後縮回 355px

順帶補上的防護:視窗大小改變時重新夾限面板、把飄出去的註解視窗拉回、收掉座標已失準的選單;
另加 `ResizeObserver` 盯著版面,因為 iframe 載入時寬度有約 1.7 秒的過渡期
(實測 `1290 → 860`),在過渡期內開啟面板會用到錯的寬度,而初始版面變化不保證會發 `resize` 事件。

> 驗證限制:這個測試環境從父頁改變 iframe 尺寸時,iframe 內**既不發 `resize` 也不觸發 ResizeObserver**
> (`innerWidth` 卻有更新),與先前記錄的預覽窗格故障同一類。所以自動觸發那條路徑無法在此驗證;
> 手動派發 `resize` 事件確認過處理邏輯本身正確(面板由 821 正確還原成 1241)。
> **真正解決卡死問題的是「開啟面板時夾限」這條路徑,它不依賴任何事件,已完整驗證。**

回歸測試(桌面 1280 / 短螢幕 573 / 手機 375 三種寬度):標記、署名留言、提議修改、三個選單貼齊與互斥、
提示條調色盤、無橫向捲軸、把手可抓 —— 全數通過。

**授權檔整理**:`LICENSE` 最上方補 `SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0`,
並把夾在條款中間的中文摘要搬到獨立的 `LICENSE-zh.md` —— 現在 `LICENSE` 與 PolyForm 原文
**逐字 100% 一致**(已用官方原文比對驗證),唯一增補是 SPDX 標頭與 `Required Notice:` 版權行。
後者不能刪:PolyForm 的 Notices 一節明文要求散布時附上「任何以 `Required Notice:` 開頭的純文字行」,
那是這份授權指定的版權聲明載體。

> **更正一個我先前的錯誤判斷**:我原本說 GitHub 回報 `NOASSERTION` 是因為中文摘要讓全文比對對不上。
> 查證後不是 —— GitHub 的偵測範圍就是 choosealicense.com 的 47 個授權(全部是開源授權),
> **PolyForm 屬於 source-available,根本不在名單裡**,所以 About 側欄永遠不會顯示這個授權,
> 清理檔案或加 SPDX 標頭都改變不了。搬走中文摘要仍然值得做,但理由是另外兩個:
> ①避免摘要夾在條款中間,讓授權文件的效力範圍變得可爭論(摘要本身已寫明以英文為準,但位置容易誤解)
> ②SPDX 掃描工具(FOSSA／ScanCode／reuse)比對的是全文,乾淨的檔案才認得出來。

## v3.9 — 2026-08-03（UI/UX 全面稽核後的修正）

使用者回報實際操作時看到排版與透明問題。做了一次系統性實機稽核（逐一開啟每個浮動元件，量測背景透明度、CSS 變數解析、溢出、重疊，涵蓋深淺兩主題、中英雙語、桌面與手機寬度），找到兩個真問題：

- **修正：提示條（toast）背景全透明、文字全黑** — `#annoToast` 只被加進了「文字擷取排除」與「輸出時清除」兩份清單，卻**漏加進定義調色盤變數的那兩條規則**，導致 `var(--as)`／`var(--atx)`／`var(--aacc)` 全部無法解析：背景成為 `rgba(0,0,0,0)`、文字與邊框 fallback 成純黑。深色模式下等於一段黑字直接疊在文件上，完全沒有卡片外觀。這是 README 已載明的重複性錯誤（v2.3、v3.1 同一種），**我在 v3.2 新增 toast 時又踩了第三次** — README 的警語已更新為「三次了」
- **修正：三個選單蓋住標註清單** — `.anno-menu` 原本固定 `right:18px;bottom:64px`，跟面板同一個錨點，所以使用者點的是面板頂端的按鈕、選單卻蓋在面板中下段（實測遮擋達 282×254）。新增 `placeMenu()` 把選單貼在**觸發按鈕正下方 6px**，水平對齊按鈕並夾限在視窗內（下方空間不足時自動往上開）。遮擋範圍降到 108px，且符合一般下拉選單的預期行為

稽核過但**確認正常**的部分（記錄下來避免日後重複查）：面板／註解視窗／兩個 modal 的版面與溢出、長文字不產生橫向捲軸、英文較長標籤不撐破面板、手機寬度（375px）、選單文字對比、landing page 全頁溢出與各區塊背景。

另有兩處經查為**誤報**：`.priv .inner` 的背景是 `background-image` 漸層（所以 `backgroundColor` 本來就會回傳透明）；選單容器的 `color` 雖為黑色，但其中所有文字元素都自帶顏色，實測對比值足夠。

## Landing page — 2026-08-03（定位分層，7 角色實測的最後一項）
實測結果是分裂的：**2 位技術角色對 AI 定位反應極好**（AI 工程師「直接戳中痛點」、Vibe coder「這個懂我」「難得整頁沒有出現我看不懂的工程術語」），但**5 位非技術角色覺得被排除**（「這是工程師的工具，不是給我用的」「馬上覺得這是大人上班用的東西」）。高中生更點出關鍵：「只看首頁會誤以為這是很技術性、跟我沒關係的工具，**實際打開後才發現基本畫重點功能其實蠻親民**」——產品比它的行銷頁親民，是 landing page 自己把人嚇跑了。

作法是**分層而非弱化**（AI 敘事是護城河，且技術訪客反應最強，不能砍）：

- **新增「兩種用法」分岔段落，緊接在 hero 之後**（最大曝光位置）——讓每種訪客在往下讀之前先認出自己：
  - 「跟同事一起看一份文件」／標註「完全不需要懂技術」——會議記錄、草稿、公告；不用安裝；可列印存 PDF
  - 「審閱 AI 寫的報告」／標上 **進階** 徽章＋「如果你常跟 Claude／GPT／Cursor 工作」
  - 底下一句關鍵保證：「兩種用的是同一個檔案、同一套按鈕。**如果第二種不是你，完全可以直接忽略**——下面那些不看也能把第一種做完。」
- **AI 閉環段落標為進階並明講可跳過**：eyebrow 改為「AI 審閱閉環 · 進階」，開頭加粗「如果不是你，跳過就好，一般的審閱用不到這些」
- **問題段落不再以 AI 為唯一框架**：標題從「AI 現在大量產出 HTML 報告，審閱它們卻還卡在雲端」改為「HTML 沒辦法像 PDF 那樣把註解存在檔案裡」，AI 降為需求推力之一（與匯出報表、儀表板、會議記錄並列）
- **隱私段落的例子加入一般辦公室與學校**（原本只有金融／政府／法務／醫院，國中生因此覺得「這是大人上班用的東西」）
- **og:description 不再只講 AI**（社群分享時的第一印象同樣要涵蓋兩種用法）
- 導覽列加入「兩種用法」，移除較不重要的「功能」

## v3.8 — 2026-08-03（跑完整 AI 閉環實測後的修正）
拿一份刻意植入 AI 常見缺陷的報告（無來源數據、表格前後矛盾、可疑的八倍成長斷言、混進俄文的語言洩漏、結論段贅詞）跑完整條閉環：審閱 → 產生修改指令 → 由 AI 依指令改寫 → 載入修改版 → 比較版本。閉環本身如預期運作，但**只有跑完整流程才會發現的一個問題**：

- **修正「AI 已照提議改好，工具卻還顯示成待處理」**：
  - 情境：審閱者提議「основной 玩家 → 主要玩家」，AI 照做了。重新開啟修改版後，模糊定位正確把標註移到「主要玩家」上，但那則提議仍顯示成待處理，「✓ 接受(套用)」還是亮的
  - 問題：按下去只是把「主要玩家」換成「主要玩家」（no-op），卻會寫入一筆稽核紀錄「套用提議的修改:『основной 玩家』→『主要玩家』」—— **原值已經過期，這筆紀錄是誤導的**
  - 修法：`renderAll` 重新定位後比對「文件現在這段文字」與提議內容，相同即標記 `sugFulfilled`；此時改顯示「✓ 已經照你的提議改好了」、隱藏接受鈕、只留「✓ 確認對了，收掉提議」
  - 未被實現的提議行為完全不變（已回歸驗證）

閉環其餘部分的實測結果（全數符合設計）：5 則標註在 AI 改寫後全部存活 —— 2 則原文未動的精確命中、2 則被改寫的**由模糊定位跟著移到新文字上**、1 則原文被整段刪除的正確降級為 orphan 並顯示「留言都還在」；修改指令含脈絡與兩則確定性「提議改成」指令；比較版本以 +146/-50 精確標出 AI 這輪每一處改動。

## v3.7 — 2026-08-03（可用性實測修正：第六批 — PDF 匯出）
大學生：「三個選項裡只有第一個我看得懂，issue／PR／JSON 這些字對我沒有意義，而**我最想要的 PDF 或 Word 反而沒有**」，並建議「加一個『匯出 PDF（列印）』放第一個」。

- **列印／存 PDF 提升為匯出選單第一項**：這個能力其實 v1 就有（`beforeprint` 會自動替標記加編號、文末附上完整註解對照表），但**只寫在說明的「要知道的事」裡，沒有任何人找得到**。現在直接放進 📤 匯出選單最上方
  - 點下去開啟瀏覽器列印視窗，提示條同時教「在『目的地』選『另存為 PDF』就能存成 PDF 檔」
  - 列印產出＝整份文件＋標記編號＋文末所有註解（含署名時間）；印完自動清除頁面上的列印用元素
  - 含標記版也帶著這個功能，收件人同樣能存 PDF
- **釐清兩種「輸出」的差別**（原本兩者都叫報告、都用 📄，容易混淆）：
  - `🖨 列印／存成 PDF` — 整份文件加上標記
  - `📄 一頁報告` → 改名 **`📄 一頁標註清單`**，副標明講「只有標註跟留言（不含文件）」
- **Markdown／JSON 說明改白話**（多位受測者「完全不知道是什麼、也不敢亂點」）：改成「純文字待辦清單，給專案工具用（**不用的人可以忽略**）」「給程式或 AI 讀的資料檔（**不用的人可以忽略**）」——明確給非技術使用者一個「可以跳過」的許可
- 說明內文同步：匯出那條列出四種格式，疑難那條改寫成「用 📤 匯出 → 🖨 列印／存成 PDF（或直接按 Ctrl+P）」

## v3.6 — 2026-08-03（可用性實測修正：第五批 — 「建議修改 vs 編輯」改名，4/7 命中）
4 位受測者搞不清楚這兩個功能。大學生與高中生都指出**同一個根因：兩個都用鉛筆圖示**——「兩個圖示都是筆、名字都在講『改』，我一直搞不清差別」「我完全猜不出差別，只能兩個都點下去試」。他們也直接提了改名建議（「提議這樣改（對方可接受）」／「我直接改掉」）。

- **換圖示斷開視覺混淆**：建議修改的 `✎` 改為 **💡**（燈泡＝提議），與直接編輯的 `✏️`（鉛筆＝自己動手寫）在視覺上再也不會被看成同一類
- **名稱直接把差別寫進按鈕**（不再只靠 tooltip）：
  - `✎ 建議修改文字` → **`💡 提議改成…（不會馬上生效，要對方同意）`**
  - `✏️ 編輯` → **`✏️ 直接改字`**；完成狀態 `✔ 完成編輯` → `✔ 完成修改`
- **tooltip 改用 Word 類比**（一般職員原話建議）：「像 Word 的『建議修訂』— 你提出這段該改成什麼，但文件不會馬上被改；對方（或你自己）可以接受或拒絕，也能交給 AI 照著改」／「我自己直接改文件的文字，按下去就立即生效，不用等任何人同意、也不會留下標註」
- **中文術語全面統一為「提議」**：儲存提議／移除提議／提議改成／套用提議的修改／💡 提議 徽章，連 🤖 修改指令輸出的標籤與「照抄」規則都同步（實測確認 prompt 內已無舊詞殘留）
- 說明內文、首次使用引導條、面板徽章、稽核紀錄全部同步更新
- **順手清掉 v2.4 的殘留**：頂欄編輯鈕在 v2.4 移除後，`SHELL_I18N` 仍留著 `edit`/`editDone` 死鍵，一併刪除
- **修正一個我自己打錯的簡體字**：tooltip 裡誤打「马上」，已改回「馬上」，並全檔掃過 12 個高風險簡體字確認無其他殘留

## v3.5 — 2026-08-03（可用性實測修正：第四批 — 說明重排，7/7 命中率最高的一項）
7 個角色**全部**都說使用說明太長、看兩段就放棄：一般職員「一大片密密麻麻的條列」、國中生「三十幾行的小字，看兩行就關掉，等於沒看說明」、高中生「全部塞在一起，沒有分頁也沒有圖」、大學生「七個標題二十幾條 bullet，還混了技術細節」、Vibe coder「掃兩段就關掉」。

- **示意圖移到最前面、不用點開就看得到**：原本兩張圖埋在摺疊區塊中段（高中生甚至以為「沒有圖」）。現在改成「30 秒看懂」直接顯示在說明最上方 —— 使用者不必點任何東西就能看完核心用法
- **長條列拆成三段漸進式展開**（原本一次攤開 24 條）：
  - 📖 **基本操作**（9 條）開檔、標記、留言、傳給對方 —— 一般使用者只需要這段
  - ⚙️ **進階功能**（7 條）建議修改、AI 修改指令、匯出、比較版本
  - 🔧 **疑難與細節**（8 條）標記跑掉了？換電腦？手機能看嗎？
  - 每段標題都附一行白話副標，一眼看出該不該點開；預設全部收合
- **技術術語全面白話化**（使用者逐字點名看不懂的詞）：
  - 「重新對位」→「自動找回原本的位置」
  - 「自包含 HTML」→「圖片直接內嵌在檔案裡的 HTML」
  - 「螢光段落會失去定位」→「標記接不回原本的位置，**你的留言完全沒有不見**」
  - 疑難段落改成以「症狀」開頭（看到 ⚠ / 看到 ≈ / 圖片沒顯示），使用者能直接對照自己遇到的狀況
- **補上先前未寫入說明的新功能**：✍ 直接貼上文字、留言的「你是：」署名欄、🤖 比較版本、✎ 建議修改的 diff 與「↩ 還原成原文」、📤 三種匯出格式、🎨 切換預設不影響既有標註
- 中英雙語同步重寫；字數精簡至約 1,550 字（原本更長且全部一次攤開）

## v3.4 — 2026-08-03（可用性實測修正：第三批 — 拆掉「我沒有 HTML 檔」這道門檻）
實測中一般職員與大學生都卡在同一個地方：他們的文件都在 Word／Google 文件／PDF，而 landing 與工具從頭到尾都假設使用者手上已經有 HTML 檔。大學生的原話：「看完不知道自己能不能用」——這是還沒進門就被擋掉的第一道牆，比任何功能問題都前面。

- **新增「✍ 直接貼上文字」入口（編輯器首頁）**：與其教人轉檔，不如讓人跳過轉檔
  - 從 Word／Google 文件／PDF／email 任何地方複製內容貼進來，即可開始標註
  - 純文字自動排版成一份文件（空行分段、單行換行保留、附標題與易讀樣式）；貼 HTML 原始碼則照原樣顯示（自動偵測）
  - 可自訂文件名稱；留空則取開頭文字當標題（標題是標記存取的索引，不能為空）
  - 貼上的文件走完整流程：標註、留言、含標記版來回合併、AI 修改指令全部正常
- **新增「我手上沒有 HTML 檔?」說明面板**：Word／Google 文件／PDF／AI 產出報告／網頁 五種來源各一行具體步驟，結尾直接建議「最快的方式是用貼上文字」
- **Landing page 新增「你可能想問」段落**：標題直接用使用者的原話「可是我的文件都在 Word 和 Google 文件裡。」，並在 hero 首屏的 micro-CTA 加上「沒有 HTML 檔?直接貼上文字」捷徑連結與導覽列項目 —— 讓非技術訪客在決定要不要下載之前就知道自己用得上
- 雙語、Esc 可關、無版面溢出，皆已實測

## v3.3 — 2026-08-03（可用性實測修正：第二批 — 信任與語意正確性）
- **接受建議必留稽核痕跡、且可還原**（會計角色的紅線：「金額被改過但查不到原值、也不知道誰改的，在我們公司過不了複核」）
  - 套用後在對話串寫入一則系統紀錄：`✓ 套用建議修改：「原值」→「新值」`，含套用者與時間 → 痕跡會跟著含標記版傳給別人，也會出現在報告／Markdown／JSON 匯出
  - 新增 **↩ 還原成原文**：改回原值、建議退回「未套用」狀態，並同樣留下還原紀錄
  - 系統紀錄用虛線框與一般留言區隔
- **建議修改改為新舊 diff 對照**（會計＋大學生都要求）：不再只丟一整段新字串讓人自己逐字比對，改為**刪除線＝原文、底線＝改成的字**（複用 v3.1 版本比對的 diff 引擎，零新增依賴）。套用前後都看得到對照
- **切換 🎨 預設不再偷改既有標註的語意**（Vibe coding 工程師回報：他標的「有問題」＝這功能該砍，切預設後變成「事實錯誤／幻覺」，**還照樣匯出到 AI 指令裡**）
  - 建立標註時把「當下這個顏色代表什麼」快照進 `label`，顯示與所有匯出一律優先使用該快照（新增 `annoLabel()`，共 6 處改用）
  - 切換時明確提示「原有 N 筆會保留當初分類，只有之後新標的才用新意義」
- **補上 AI 閉環最容易斷掉的一環**（Vibe coding 工程師列為第一名）：原本 prompt 只含片段、指示卻要 AI「回傳完整 HTML」，使用者不知道還要自己附原檔
  - 修改指令視窗新增 **☐ 含文件全文** 開關：勾選後把全文附在指令末端，整段貼過去即可運作
  - 說明文字直接寫明「記得連同你的 HTML 檔一起給 AI，或按「含文件全文」」
- **「找不到原文位置」改成不會嚇人的說法**（會計：「我第一個反應是我把檔案弄壞了嗎？我的留言是不是要不見了？」）：警語本身就帶保證 →「⚠ 這段原文已被改動，標記接不回去（**留言都還在**）」，並加上 tooltip 解釋原因與補救方式
- **Esc 改為分層關閉**（大學生：「Esc 把整個標註清單面板一起關掉了」）：選單 → 視窗 → 留言框 → 面板，一次只收最上層
- **「複製」立即回饋**（Vibe coding 工程師：「只看到文字變藍，不確定到底複製成功沒有」）：不等 clipboard promise，按下即顯示「已複製 ✓」＋提示條
- **「接受(套用)」加說明**（Vibe coding 工程師：「我不敢按」）：按鈕上方寫明「會直接把文件這段換成建議文字，原文會留在留言紀錄裡，隨時可以還原」
- **篩選 chip 標籤加寬**（76px → 132px）：AI 預設的長標籤不再被截成「缺來源／待…」讓人靠猜

## v3.2 — 2026-07-27（7 角色可用性實測後的修正：第一批）
以 7 個角色（AI 工程師／Vibe coding 工程師／一般職員／會計業務／國中生／高中生／大學生）盲測 landing page + 編輯器的結果為依據，先修「命中率最高、且屬明確缺陷」的部分：

- **署名機制重做（實測 6/6 留過言的人都中，最致命）**：舊版用 `prompt()` 問名字，在部分環境（sandbox／file://）會被瀏覽器直接擋掉並回傳 `null` → 存入空字串後「**永遠不再詢問**」→ 所有留言永久掛「匿名」且使用者無從發現。`👤 名稱` 同樣依賴 `prompt()`，多位使用者回報「按了完全沒反應」。
  - 改為**留言視窗內嵌「你是：____」欄位**（邊打邊存，不依賴任何瀏覽器彈窗）
  - 沒填名字時**擋下送出**並聚焦欄位＋紅框提示，不再默默送出「匿名」
  - `👤 名稱` 改為開啟最近一則標註的留言視窗並聚焦署名欄；文件上還沒有標註時用提示條說明
- **下載／匯出加上完成提示（實測 5/7 困惑）**：原本按下去畫面毫無變化，使用者不確定成功與否（「不敢傳給同學」「一定會一直重複按」）。新增畫面內提示條，明確顯示「✓ 已存到『下載』資料夾：檔名（大小）」，涵蓋含標記版／一頁報告／Markdown／JSON／修改指令 .md 全部路徑
- **含標記版檔案變大的說明**：實測有人看到 2.7KB→97KB 以為壞了。下載後的提示條加一段說明「工具一起包進去了，對方不用安裝、直接開就能看與回覆，傳回來會自動合併」
- **建議修改的輸入框改為預填＋全選**（2 人因此任務失敗放棄）：原本只 `focus()`，游標停在原文末端，使用者直接打字變成「原句＋新句」黏在一起（有人重試一次仍失敗，最後按「移除建議」放棄）。現在 `select()`，直接打字即整段替換
- **英文介面頂欄不再擠爆**（2 人回報，一人「以為切不回中文」）：`#topbar` 加 `flex-wrap`、`#docName` 加 `min-width`，英文較長的標籤會換行而不是把語言鈕推出畫面、產生橫向捲軸
- **面板版面修正**：`#annoList` 給 `min-height`、其餘固定高度的子元件（標頭／引導條／篩選列／底部說明）一律 `flex:none` —— 修掉「打開面板看不到自己剛寫的留言，以為沒存到」（v2.9 加的引導條讓這個問題變嚴重）與「底部說明文字溢出面板、壓在正文上」
- **修掉兩處我自己造成的說明文件錯誤**：①v2.4 把「✏️ 編輯文字」從頂欄移進面板後，說明仍寫「按上方『✏️ 編輯文字』」，害使用者在頂欄找不到；②說明完全沒提「✎ 建議修改」（有使用者說「我認真讀說明也學不到那個功能」）。現在兩者並列說明、明確對比「提議（可接受／拒絕）」與「直接改（立刻生效）」

## v3.1 — 2026-07-21（第二梯 #3:版本比對）
- **🔍 比較版本**:看這一輪(尤其是 AI 那一輪)文件實際改了哪裡 — 紅色刪除線是舊文字、綠色底線是新文字，並附「+新增字數 / -刪除字數」統計
- **基準版本自動記錄**：①第一次開啟文件時存一份「初見樣貌」；②每次按🤖產生修改指令時，自動把「送去給 AI 之前」的樣子存成基準。之後開啟 AI 回傳的修改版(同標題→同 key)按「比較版本」，就能看到 AI 這輪實際改了什麼
- 純本機、不烙進含標記版——這是個人比對輔助，不是跨人協作資料（跟留言/標註的定位不同）
- **零依賴 diff 引擎**：自寫字詞級 Myers 演算法（O(N·D)，對齊 fuzzy anchoring 引用過的同一套演算法家族），差異過大或文件過大時自動退回「共同前綴/後綴＋中段整塊替換」，不會卡頓
- **🤖 按鈕改為選單**（不增加面板按鈕數）：原本直接開啟修改指令，現在點開後有兩個選項——「產生修改指令」／「比較版本」，把 AI 修改迴圈的兩個動作收在同一入口，回應「功能有點多」的回饋
- 順手把修改指令視窗的 modal 外觀抽成共用 class(`.anno-modal-ov`/`.anno-modal-bx`)，比較版本視窗共用同一套外觀，不必重寫
- **測試中抓到並修掉兩個 bug**：①比較版本視窗背景透明——調色盤 CSS 變數定義清單忘了加新的 `#annoDiffOverlay`（跟 v2.3 那次同一種疏漏，選單/視窗類元件新增時都要記得加）；②**中文 diff 幾乎沒用**——原本斷詞用 `\s+|\S+`，中文沒有空白分字，整句會被當一個 token，導致改一個字就整句被標成整塊刪除+新增。已改為中英混合斷詞（拉丁字母/數字連續算一詞，其餘逐字元切開），中文 diff 現在能精準到字

## v3.0 — 2026-07-21（使用說明加視覺示意圖）
- 首頁「📖 完整使用說明」新增兩張輕量示意圖(純 HTML/CSS 組成,無外部圖檔、不會過期,隨深淺色主題自動配色):
  - **標記與註解**節:「選字 → 挑顏色 → 彈出視窗寫留言」三步驟示意
  - **來回協作**節:「你 → 傳出 → 同事 → 傳回 → 合併後」的檔案來回流程示意
- 選在編輯器首頁而非 landing page —— 教操作的最佳時機是使用者已打開工具的當下,而非還在決定要不要下載的行銷頁;純 CSS 而非真實截圖,是因為 UI 仍在快速迭代,截圖容易過期、維護成本高

## v2.9 — 2026-07-21（易用性:功能變多後的上手引導）
- **首次使用提示**:第一次打開標註面板時,在動作列下方顯示一次性提示——「核心用法：選字→標色→留言;進階功能(匯出／修改指令／建議修改)需要時再用」。建立第一則標註或按「知道了」即消失,靠 `annoReader::onboarded` 跨所有文件只顯示一次
- **釐清 ✏️ 編輯 vs ✎ 建議修改的差異**（先前使用者反映兩者感覺重複）:
  - ✏️ 編輯 = 直接改字、立即生效、不建立標註 —— 適合快速修錯字
  - ✎ 建議修改 = 對某段文字提議修改、不立即生效、可接受/拒絕、可餵給 AI —— 適合審閱情境
  - 兩者 tooltip 都改寫成互相對照的說明,提示引導文字裡也講一次
- 純文案與提示調整,不涉及資料結構變動

## v2.8 — 2026-07-21（第二梯 #2:建議修訂）
- **每則標註可帶一個「建議修改」**(提議的新文字):註解視窗新增 ✎ 建議修改 → 輸入替代文字 → 儲存
- **接受/移除**:接受即把標記文字精確替換為建議文字(走編輯/persist,並靠 v2.7 模糊比對讓其他標註自動重定位);移除則丟棄提議。orphan 標註無法直接套用(接受鈕停用並提示)
- **與 🤖 修改指令的綜效(核心)**:帶建議的標註在 prompt 中輸出成確定性的「建議改為:"新文字"」,並在指示尾端加「請精確替換、照抄不要改寫」→ AI 一字不差套用,而非讀留言去猜
- 建議隨含標記版傳遞(mergeIn 合併時保留)、也進 Markdown(`→ 建議改為`)與 JSON(`suggestion` 欄位)
- 視覺:帶建議的標記加實線底線、清單顯示「✎ 建議」徽章 + 提議文字
- 這是「人給精確修正 vs 人給方向、AI 執行」中缺的那一半;#5(跨改寫存活)+ #6(精確回饋)+ #2(打包回餵)三塊拼圖到齊

## v2.7 — 2026-07-21（第二梯 #1:模糊比對）
- **原文被改寫後仍救回標註**（讓 AI 修改閉環跨輪可用）:精確比對失敗時不再直接變 orphan,改走模糊救回
  - (A) 上下文重定位:用最靠近引文的前後文短錨點框出「被改寫段落」現在的位置(前後文還在時最準、最快)
  - (B) 舊位置附近 bigram Dice 相似度滑窗(局部,相似度 ≥0.5 採用)
  - (C) 全文滑窗(相似度 ≥0.55;文件 >200KB 則略過防卡頓)
- 模糊命中標為 `approx`:標記加虛線底線 + ≈ 上標、清單以「≈」與琥珀色標示、hover 提示「位置為近似,請確認標的」— 誠實告知使用者這是重新定位的結果
- 純前端、零依賴(自寫輕量 Dice,不引入 diff-match-patch);短於 6 字的引文不做模糊比對以免亂配
- 精確命中與多重消歧路徑不變(approx=false),無回歸

## v2.6 — 2026-07-21
- **篩選列一致化**:搜尋框獨立一行;四個顏色篩選 + 未解決統一為同一種 pill chip(共用外框、高度、開/關狀態)
- 顏色篩選 chip 改為「色點 + 意義標籤」(標籤取自目前圖例,過長以 … 截斷),一眼看出各色代表什麼、也對齊動作列的 icon+文字風格
- 「未解決」加上 ○ icon → `○ 未解決`,與 ✓ 已解決語意呼應
- `.fdot` 全面改為 `.fchip`;顏色篩選點擊 hook 由 `.fdot` 改綁 `.fcolor`,`applyLegend` 同步填入 chip 標籤

## v2.5 — 2026-07-21
- **面板動作鈕統一為「icon + 文字」** —— 先前混雜(👤/🎨/🌐 純 icon、清空純文字、其餘 icon+文字)。全部改為一致格式:👤 名稱、🌐 EN／中文、⬇ 含標記版、📤 匯出、🤖 修改指令、🎨 顏色、✏️ 編輯、🗑 清空
- 標記結構統一:所有面板動作鈕內容改由 `applyI18n` 以 `textContent` 填入完整標籤(移除內嵌 icon 與 `annoExportLab`/`annoPromptLab` 的 span),避免同一列有兩種寫法

## v2.4 — 2026-07-21
- **消除重複功能鈕、重新規劃按鈕位置**:先前「下載含標記版／編輯文字／語言」三個功能在頂欄與標註面板各出現一次(因為面板須自給自足以支援獨立開啟的含標記版)
- 新分工:**頂欄只管導覽**(🏠 回列表、📂 開啟 HTML)＋全域語言;**面板是所有文件/標註動作的唯一入口**(名稱、含標記版、匯出、修改指令、圖例、編輯、清空)
- 頂欄移除 ⬇下載含標記版、✏️編輯文字(面板已有)
- 語言鈕:頂欄保留;**面板的 🌐 偵測到自己在閱讀器內就隱藏,獨立開啟含標記版時(無外層頂欄)才顯示** → 任何畫面每個功能都只剩一顆鈕

## v2.3 — 2026-07-21
- **修正:選單與修改指令視窗背景透明的 bug** —— `.anno-menu`、`#annoPromptOverlay`／`#annoPromptBox` 沒被納入定義調色盤 CSS 變數的選擇器清單,導致 `var(--as)` 等變數未定義、背景 fallback 成透明,文字浮在文件上看不清楚。已把這些元素加入變數定義規則,背景恢復不透明
- **每顆按鈕都加上滑鼠懸停說明(tooltip),用非技術語言撰寫** —— 涵蓋頂欄(回列表／開啟／編輯／下載含標記版)、標註面板(👤名稱／⬇含標記版／📤匯出／🤖修改指令／🎨圖例／✏️編輯／清空／🖍清單開合)、以及註解視窗(✓解決／刪除／送出)。中英雙語
- 例:🤖 修改指令 →「把你的標註整理成一段指令,貼給 AI 讓它照著修改文件」

## v2.2 — 2026-07-20
- **📤 匯出選單**:整併輸出動作 —— 一頁報告(HTML)／**Markdown**／**JSON**。原本獨立的 📄報告 按鈕收進此選單,面板更清爽
- **Markdown**:任務清單格式(`- [ ]` / `- [x]` 依解決狀態），色點＋分類＋引文＋留言子項，可直接貼進 GitHub issue／Notion／PR
- **JSON**:結構化資料（含 id、color、category、quote、prefix/suffix、resolved、orphan、comments 含作者/時間戳），供程式或 LLM 讀取，帶完整 anchoring 上下文
- 與 📄報告、🤖修改指令共用同一套標註聚合邏輯；分類標籤沿用目前的圖例預設（AI 報告審閱模式下即為缺陷分類）
- 兩個選單（🎨圖例、📤匯出）互斥開合，Esc／點外部關閉，隨引擎進含標記版並在重載時清乾淨

## v2.1 — 2026-07-20
- **圖例預設集**:🎨 從「直接跳 prompt」升級為選單 —— 一般審閱 / **AI 報告審閱** / 自訂
- AI 報告審閱預設把四色對齊 AI 生成內容的常見缺陷:🔴 事實錯誤／幻覺、🟡 缺來源／待查證、🟢 已查證、🔵 語氣／冗長／格式。讓回饋天生結構化,直接餵進 🤖 修改指令的分類欄
- **語言感知**:general/ai 預設的標籤跟著介面語言翻譯;自訂與檔案內帶的圖例維持不動
- 預設集選擇存 `annoReader::legendPreset`,標籤字串隨含標記版傳給收件人;向後相容既有的自訂圖例(視為 custom 模式)

## v2.0 — 2026-07-20
- **🤖 修改指令(Revision prompt)**:面板新增按鈕,一鍵把所有「未解決」標註聚合成結構化 prompt —— 含文件名、每則的原文引用 + 前後脈絡（…前文〖標記〗後文…）+ 分類 + 留言串 + 給 LLM 的修改指示，可直接複製或下載 .md，貼回 Claude／GPT／Cursor 讓 AI 改文件
- 這是「審閱 AI 生成報告 → 回餵重生」閉環的核心；脈絡由 v1.9 的 anchoring 升級提供，AI 定位更準
- Modal 走既有 CSS 變數（深淺色一致），隨引擎進含標記版；複製走 clipboard API + `execCommand` 雙保險，離線／受限環境也能手動全選複製
- 雙語 prompt 骨架（介面為中文則產中文指示、英文則產英文），留言原文照舊

## v1.9 — 2026-07-20
- **anchoring 改為引文為主、位置為輔**(對齊 W3C TextQuoteSelector 精神):新標註加存前後各 32 字上下文（`prefix`/`suffix`）
- 定位邏輯:①原位置精準命中即採用 →②全文唯一出現即定位 →③多處重複時以上下文吻合度優先、位置就近決勝。解決「同一段文字出現多次時定位到錯處」與「前文一改就整批位移失效」兩大脆弱點
- **向後相容**:舊標註(無上下文)自動退回位置就近,且成功定位後就地補上 `prefix`/`suffix`,存檔／打包時順勢升級為穩健格式
- 模糊比對(改寫後仍救回)留待後續版本

## v1.8 — 2026-07-20
- 雙語系統（繁中／英文）：外殼與標註引擎各持一份字典，語言偏好存 `annoReader::lang`，未設定時依 `navigator.language` 自動偵測（zh 開頭 → 中文，其餘英文）
- 切換入口：頂欄「🌐 EN／中文」按鈕＋標註面板 🌐 鈕，兩邊即時連動（`__scopeSetLang` ↔ `__annoSetLang`，fromX 旗標防循環）
- 字典跟著 `ENGINE.toString()` 進含標記版 → 收件人依自己的語言偏好顯示介面（傳英文檔給國外同事、他傳回你仍看中文）
- 未自訂圖例時，四色預設意義跟著語言切換；已自訂或檔案帶圖例則保持不動
- 下載檔名後綴依語言（`_標記版`／`_annotated`、`_標註報告`／`_report`），回傳合併時兩種後綴都能剝除

## v1.7 — 2026-07-20
- 含標記版加入 `#annoStaticApp` 靜態標註對照表：無 JavaScript 的環境（如手機通訊軟體預覽）也能看到標記內容；正常瀏覽器開啟時由引擎移除、接管互動

## v1.6 — 2026-07-20
- 註解視窗加「⠿ 可拖曳」把手（滑鼠/觸控通用，夾限在畫面內）與 ✕ 關閉鈕
- 「✏️ 編輯文字」提升到頂部工具列（原本只在 🖍 面板內，不易發現）

## v1.5 — 2026-07-20
- 修正：文件自帶舊版標註 CSS（如分配書 v6.3 內建版）壓扁新版標籤按鈕 → 引擎樣式加防禦性尺寸宣告＋載入時拆除文件內舊標註規則（regex 迴圈取代，處理相鄰規則共用 `}` 的漏刪問題）

## v1.4 — 2026-07-20（開始標示版本號）
- 顏色按鈕從純色圓點改為「色點＋文字標籤」常駐顯示（選取列與註解框）
- ✏️ 編輯模式：直接修改文件內文，完成後標記自動重新對位、改動存入快取並跟著含標記版輸出
- 首頁使用說明重排為標題＋清單結構（修正文字牆）
- 工具列加版本號

## 未標號時期 — 2026-07-20 單日演進
1. **初版**：閱讀器＋四色標註＋註解＋清單面板＋localStorage 儲存＋JSON 匯出入＋「⬇ 含標記版」（文件＋標記＋引擎打包，零安裝傳遞）＋載入時拆除 script 的安全設計
2. **協作版**：註解升級為對話串（署名＋時間）、✓解決、留言層級合併、墓碑防復活、儲存 key 改用文件標題 hash（來回傳遞檔名變了標記仍接得上）、舊分配書標記自動搬移
3. **追蹤版**：清單篩選（關鍵字/顏色/未解決）、未解決計數徽章、📄 一頁式標註報告、回覆草稿防丟
4. **周邊**：🎨 自訂四色圖例（跟著檔案傳遞）、列印附編號註解對照表、手機/平板觸控選字、深淺色自動切換、清單面板拖曳縮放
5. **精簡**：移除 JSON 匯出入按鈕（含標記版已覆蓋其用途）

## 起源
2026-07-15 起為《AgentHub 工作總分配書》先做了文件內嵌標註功能，後抽出為通用工具「HTML 標註閱讀器」，2026-07-20 改名 HTMLScope 移入 side project 統一管理。
